export const favoriteSelector = (state) => state.favorites.favorites;
export const addFavoriteSelector = (state) => state.favorites.addFavoriteAdvert;
export const deleteFavoriteSelector = (state) => state.favorites.deleteFavoriteAdvert;
